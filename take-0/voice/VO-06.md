# VO-06 - Receipt, protected action, independent observation

Scene window   01:57.5 - 02:36.0  (38.5s of picture)
Target speech  33.0s at 2.7 words/sec
Slack          5.5s of silence for visual comprehension
Beats          T11, T12, T13

## Text to paste into ElevenLabs

The protected mutation is EXECUTED against that receipt. The resulting invariant on the target is 105 against a bound of 130. EXECUTED is not OBSERVED. A separately authenticated, keyless observer service account read the target back at the post-mutation revision and observed alpha at 45. Two records, not one: the model's own output declares nothing. Cloud Logging, filtered to that same correlation id: the proxy request, the agent identity as the platform verified it, and the Cloud Run revision that served it.

## Per-beat alignment

  T11  01:57.5-02:10.0  (12.5s)  The protected mutation is EXECUTED against that receipt. The resulting invariant on the target is 105 against a bound of 130.
  T12  02:10.0-02:24.5  (14.5s)  EXECUTED is not OBSERVED. A separately authenticated, keyless observer service account read the target back at the post-mutation revision and observed alpha at 45. Two records, not one: the model's own output declares nothing.
  T13  02:24.5-02:36.0  (11.5s)  Cloud Logging, filtered to that same correlation id: the proxy request, the agent identity as the platform verified it, and the Cloud Run revision that served it.

## Safe shortening

Drop "Two records, not one: the model's own output declares nothing."

## Pronunciation

- Interlock - IN-ter-lock, even stress, never "inter-LOCK".
- gemini-3.5-flash - "gemini three point five flash".
- ADK - spell it: "A-D-K". MCP - "M-C-P". IAM - "I-A-M".
- us-central1 - "U-S-central-one".
- 403 / 401 - "four-oh-three" / "four-oh-one".
- ALLOW, EXECUTED, OBSERVED, WITHHOLD_SERIALIZE, ALLOW_PARALLEL - read as flat state names, no emphasis lift. They are values, not verdicts.
- Numbers 140 / 130 / 120 / 105 / 45 - natural English, no digit-by-digit.
- CONTENT_AUTHZ - "content auth-Z".

## Delivery

Documentary read, measured, no sell. Land the numbers; do not lean on them.
Full stop between proof classes - VO-04 is a wall, not a bridge.
Render as its own file. Do not produce one monolithic track.
