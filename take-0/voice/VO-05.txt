The deployed Cloud Run agent reads back its own configuration: gemini-3.5-flash, through Google ADK 1.35.1 with Vertex AI access, from an image pinned to the runtime source commit. Three Cloud Run services carried the run in us-central1: the ADK agent, the Interlock MCP proxy, and the protected target. The traversal executed while this frame was captured. Interlock returned ALLOW with a receipt digest under one correlation id. Three fail-closed controls on the same run: a forged identity header returned 403, an invalid bearer token 401, a direct call with no receipt, 403.
