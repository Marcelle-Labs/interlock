# VO-07 - Architecture and trust boundary

Scene window   02:36.0 - 03:00.5  (24.5s of picture)
Target speech  22.6s at 2.7 words/sec
Slack          1.9s of silence for visual comprehension
Beats          T14, T15

## Text to paste into ElevenLabs

Where Interlock sits. The agent calls a tool through the Interlock MCP proxy. Interlock reads revision-bound composition evidence before the mutation and returns a decision with a receipt. Cloud Run IAM establishes transport provenance. It does not establish application-role semantics inside Interlock. Agent Runtime, Agent Gateway and CONTENT_AUTHZ were not on this path.

## Per-beat alignment

  T14  02:36.0-02:48.5  (12.5s)  Where Interlock sits. The agent calls a tool through the Interlock MCP proxy. Interlock reads revision-bound composition evidence before the mutation and returns a decision with a receipt.
  T15  02:48.5-03:00.5  (12.0s)  Cloud Run IAM establishes transport provenance. It does not establish application-role semantics inside Interlock. Agent Runtime, Agent Gateway and CONTENT_AUTHZ were not on this path.

## Safe shortening

Drop "and returns a decision with a receipt"; the diagram shows it.

## Pronunciation

- Interlock - IN-ter-lock, even stress, never "inter-LOCK".
- gemini-3.5-flash - "gemini three point five flash".
- ADK - spell it: "A-D-K". MCP - "M-C-P". IAM - "I-A-M".
- us-central1 - "U-S-central-one".
- 403 / 401 - "four-oh-three" / "four-oh-one".
- ALLOW, EXECUTED, OBSERVED, WITHHOLD_SERIALIZE, ALLOW_PARALLEL - read as flat state names, no emphasis lift. They are values, not verdicts.
- Numbers 140 / 130 / 120 / 105 / 45 - natural English, no digit-by-digit.
- CONTENT_AUTHZ - "content auth-Z".

## Delivery

Documentary read, measured, no sell. Land the numbers; do not lean on them.
Full stop between proof classes - VO-04 is a wall, not a bridge.
Render as its own file. Do not produce one monolithic track.
