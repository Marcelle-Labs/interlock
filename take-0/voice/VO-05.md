# VO-05 - Google Cloud traversal

Scene window   01:09.0 - 01:57.5  (48.5s of picture)
Target speech  44.1s at 2.7 words/sec
Slack          4.4s of silence for visual comprehension
Beats          T08, T09, T10

## Text to paste into ElevenLabs

The deployed Cloud Run agent reads back its own configuration: gemini-3.5-flash, through Google ADK 1.35.1 with Vertex AI access, from an image pinned to the runtime source commit. Three Cloud Run services carried the run in us-central1: the ADK agent, the Interlock MCP proxy, and the protected target. The traversal executed while this frame was captured. Interlock returned ALLOW with a receipt digest under one correlation id. Three fail-closed controls on the same run: a forged identity header returned 403, an invalid bearer token 401, a direct call with no receipt, 403.

## Per-beat alignment

  T08  01:09.0-01:25.0  (16.0s)  The deployed Cloud Run agent reads back its own configuration: gemini-3.5-flash, through Google ADK 1.35.1 with Vertex AI access, from an image pinned to the runtime source commit.
  T09  01:25.0-01:36.5  (11.5s)  Three Cloud Run services carried the run in us-central1: the ADK agent, the Interlock MCP proxy, and the protected target.
  T10  01:36.5-01:57.5  (21.0s)  The traversal executed while this frame was captured. Interlock returned ALLOW with a receipt digest under one correlation id. Three fail-closed controls on the same run: a forged identity header returned 403, an invalid bearer token 401, a direct call with no receipt, 403.

## Safe shortening

Drop "1.35.1" from speech; it stays on screen and in the caption.

## Pronunciation

- Interlock - IN-ter-lock, even stress, never "inter-LOCK".
- gemini-3.5-flash - "gemini three point five flash".
- ADK - spell it: "A-D-K". MCP - "M-C-P". IAM - "I-A-M".
- us-central1 - "U-S-central-one".
- 403 / 401 - "four-oh-three" / "four-oh-one".
- ALLOW, EXECUTED, OBSERVED, WITHHOLD_SERIALIZE, ALLOW_PARALLEL - read as flat state names, no emphasis lift. They are values, not verdicts.
- Numbers 140 / 130 / 120 / 105 / 45 - natural English, no digit-by-digit.
- CONTENT_AUTHZ - "content auth-Z".

## Delivery

Documentary read, measured, no sell. Land the numbers; do not lean on them.
Full stop between proof classes - VO-04 is a wall, not a bridge.
Render as its own file. Do not produce one monolithic track.
